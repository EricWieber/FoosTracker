<Alloy>
    <Window id="win" title="Confirm Win" opacity="0" >
    	<View backgroundColor="black" opacity="0.7"></View>
    	<ScrollView>
    		<Label id="lbl">Are you sure</Label>
    		<Label id="prompt">"   " won?</Label>
			<Button id="confirm" title="Yes" onClick="confirm"></Button>
			
			<Button id="cancel" title="Cancel" onClick="closeWindow"></Button>
		</ScrollView>
    </Window>
</Alloy>